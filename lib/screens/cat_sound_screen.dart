import 'package:flutter/material.dart';

class CatSoundScreen extends StatelessWidget {
  const CatSoundScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
